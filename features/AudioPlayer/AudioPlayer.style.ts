import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#F2F2F0',
    borderRadius: 6,
    borderWidth: 2,
    borderColor: '#555938',
    alignItems: 'center',
  },
  start : {
    
  }
});