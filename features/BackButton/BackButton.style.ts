import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  more: {
    alignItems: 'center',
    justifyContent: 'center',
    width: 56,
    height: 56,
    borderRadius: 28,
  }
});