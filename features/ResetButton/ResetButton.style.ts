import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  player : {
    position: 'absolute',
    width: 56,
    height: 56,
    alignItems: 'center',
    justifyContent: 'center',
    left: 60,
    bottom: 40,
    borderRadius: 28,
  }
});