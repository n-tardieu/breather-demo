import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  more : {
    position: 'absolute',
    width: 76,
    height: 56,
    alignItems: 'center',
    justifyContent: 'center',
    right: 30,
    top: 40,
    borderRadius: 28,
  }
});